<div class="album py-5 bg-light">
  <div class="container">
    <div class="row <%= "sortable" if logged_in?(:site_admin) %>">
      <%= render partial: 'portf_item', collection: @portf_items %>
    </div>
  </div>
</div>